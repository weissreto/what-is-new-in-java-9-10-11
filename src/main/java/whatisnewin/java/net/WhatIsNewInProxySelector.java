package whatisnewin.java.net;

import java.net.ProxySelector;
import java.net.InetSocketAddress;

/**
 * This source file was generated by WhatIsNewInJava.<br>
 *
 * This class provides an example call to each method in class {@link ProxySelector}
 * that were newly introduced in Java versions 9, 10, 11.<br>
 *
 * {@link ProxySelector} is an old class but has new fields, constructors or methods.
 * @since 1.5
 * @see ProxySelector
 */
public final class WhatIsNewInProxySelector
{
  /**
   * Example call to new method {@link ProxySelector#of(InetSocketAddress)}.
   * @since 9
   * @see ProxySelector#of(InetSocketAddress)
   */
  public static ProxySelector of(InetSocketAddress proxyAddress)
  {
    ProxySelector result = ProxySelector.of(proxyAddress);
    return result;
  }

}

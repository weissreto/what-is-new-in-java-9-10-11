package whatisnewin.java.security;

import java.security.KeyStoreSpi;
import java.io.InputStream;
import java.io.IOException;

/**
 * This source file was generated by WhatIsNewInJava.<br>
 *
 * This class provides an example call to each method in class {@link KeyStoreSpi}
 * that were newly introduced in Java versions 9, 10, 11.<br>
 *
 * {@link KeyStoreSpi} is an old class but has new fields, constructors or methods.
 * @since 1.2
 * @see KeyStoreSpi
 */
public final class WhatIsNewInKeyStoreSpi
{
  /**
   * Example call to new method {@link KeyStoreSpi#engineProbe(InputStream)}.
   * @since 9
   * @see KeyStoreSpi#engineProbe(InputStream)
   */
  public boolean engineProbe(InputStream stream) throws IOException
  {
    KeyStoreSpi testee = $$$();

    boolean result = testee.engineProbe(stream);
    return result;
  }

  private KeyStoreSpi $$$()
  {
    return null;
  }
}
